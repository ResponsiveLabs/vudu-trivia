<div class="results">
  
  <% if @answered_right %>
  <div class="correct">
    <h3><%= t('.correct') %></h3>
    <p><%= @question.explanation %></p>
  </div>
  <% else %>
  <div class="error">
    <h3><%= t('.incorrect') %></h3>
  </div>
  <% end %>

  <% link_title = (@index + 1) < @game.questions.size ? t('.next') : t('.finish') %>
  <%= link_to link_title, next_path(@game.id), class: 'button' %>
  
</div>
