<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :earned_points %><br />
    <%= f.number_field :earned_points %>
  </div>
  <div class="field">
    <%= f.label :correct_answers_count %><br />
    <%= f.number_field :correct_answers_count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
